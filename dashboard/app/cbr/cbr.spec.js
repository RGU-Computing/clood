'use strict';

describe('cloodApp.cbr module', function() {

  beforeEach(module('cloodApp.cbr'));

  describe('cbr controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cbrCtrl = $controller('CBRCtrl');
      expect(cbrCtrl).toBeDefined();
    }));

  });
});
